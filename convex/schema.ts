import { defineSchema, defineTable } from "convex/server";
import { v } from "convex/values";

/**
 * Database schema for GTM OS
 * PRD Reference: Section 5 - Database Schema
 */
export default defineSchema({
  // Schema tables will be defined here as per PRD requirements
});
